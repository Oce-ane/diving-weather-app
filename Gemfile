# frozen_string_literal: true

source 'https://rubygems.org'

# setup app
gem 'sinatra'
gem 'dotenv'

# setup features
gem 'httparty'
gem 'json'

# setup a database
gem 'sinatra-activerecord'
gem 'pg'
gem 'pg_search'
gem 'rake'

# setup scrapper
gem 'nokogiri'
gem 'parallel'

group :test do
  gem 'rspec'
  gem 'rack-test'
end
